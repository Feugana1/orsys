# ⚠️ .env.example - Template de configuration
#
# IMPORTANT:
# 1. COPIER ce fichier en: cp .env.example .env
# 2. MODIFIER les valeurs par défaut avec vos propres données
# 3. JAMAIS committer .env (contient les secrets!)
# 4. Ajouter .env à .gitignore (déjà fait ✅)
#
# REMARQUE PÉDAGOGIQUE:
# En production réelle, ne JAMAIS stocker les secrets en clair.
# Utiliser: AWS Secrets Manager, HashiCorp Vault, Azure Key Vault, etc.

# ============================================================
# MySQL Configuration - Base de Données
# ============================================================

# Mot de passe root MySQL (utilisé lors du premier démarrage)
# ⚠️ À changer absolument en production!
# ✅ Minimum 8 caractères recommandé
MYSQL_ROOT_PASSWORD=your_root_password_here

# Nom de la base de données (créée automatiquement)
MYSQL_DATABASE=wordpress

# Utilisateur MySQL (créé automatiquement)
MYSQL_USER=wordpress

# Mot de passe de l'utilisateur WordPress
# ⚠️ À changer absolument en production!
MYSQL_PASSWORD=your_db_password_here

# ============================================================
# WordPress Configuration - Application Web
# ============================================================

# Hôte de la base de données (ne pas modifier)
# "db" = nom du service dans docker-compose.yml
WORDPRESS_DB_HOST=db

# Utilisateur WordPress (doit correspondre à MYSQL_USER)
WORDPRESS_DB_USER=wordpress

# Mot de passe (doit correspondre à MYSQL_PASSWORD)
WORDPRESS_DB_PASSWORD=your_db_password_here

# Nom de la base de données (doit correspondre à MYSQL_DATABASE)
WORDPRESS_DB_NAME=wordpress

# ============================================================
# Optionnel: Paramètres avancés
# ============================================================

# Versions des images (pour éviter les surprises)
# MYSQL_VERSION=8.0
# WORDPRESS_VERSION=6.4-apache
