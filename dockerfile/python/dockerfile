# Étape 1 : Utiliser une image de base officielle et légère
FROM python:3.9-slim

# Étape 2 : Définir des variables d'environnement
ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1 \
    PIP_DEFAULT_TIMEOUT=100

# Étape 3 : Créer et définir le répertoire de travail
WORKDIR /app

# Étape 4 : Installer les dépendances système minimales (optionnel selon besoins)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

#Étape 5 : Copier le fichier requirements et installer les dépendances Python
COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

# Étape 6 : Copier le code source de l’application
COPY . .

# Étape 7 : Créer un utilisateur non-root pour exécuter l’application (bonne pratique)
RUN useradd -m appuser
USER appuser

# Étape 8 : Exposer le port utilisé par l’application
EXPOSE 8080

# Étape 9 : Définir la commande de démarrage
CMD ["python", "server.py"]
